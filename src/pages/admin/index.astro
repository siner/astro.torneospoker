---
import Layout from "../../layouts/Layout.astro";
import { getUser } from "../../lib/auth";
const user = await getUser(Astro.request);
if (!user) {
  return Astro.redirect("/login");
}
---

<Layout title="Admin" description="Admin">Hola</Layout>
